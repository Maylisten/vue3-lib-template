import {_buildComponents} from "./components";
import {_buildCli} from "./cli";
import {_buildDoc} from "./doc";

export const buildComponents = _buildComponents;
export const buildCli = _buildCli;
export const buildDoc = _buildDoc;
