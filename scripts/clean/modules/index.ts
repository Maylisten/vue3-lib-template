import {_cleanComponents} from "./components";
import {_cleanCli} from "./cli";
import {_cleanDoc} from "./doc";

export const cleanComponents = _cleanComponents;
export const cleanCli = _cleanCli;
export const cleanDoc = _cleanDoc;
